{
  "name": "pi-agent-api",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Tiny logging API for the Pi Agent web app — writes user requests to Postgres on Railway",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
